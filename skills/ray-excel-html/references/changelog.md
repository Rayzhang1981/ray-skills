# 变更记录（按需加载——查版本史时再读）

> 2026-09-16 由 SKILL.md 迁出（体积收敛：est 9,533 → 7,4xx）；内容逐字保留，未改写。

## 维护记录

| 日期 | 变更 |
|------|------|
| 2026-09-18 | v2.6.0：**新增 E 门（视口机检）＋ D 门机器代偿路径**（双控数字化文化墙设计任务回流，用户批「推荐档」）。①**Step 5.5 四道门 → 五道门**：新增 **E 视口机检**（各档位无横向溢出 ＋ **逐变体**均可辨），铁律补 `D 不蕴含 E`（视觉可读 ≠ 各视口都装得下、各变体都看得出区别）。**缺口实证**：此前四门里**没有任何一门管视口与变体**，而四套风格变体的板/墙可辨性实测有的通过、有的逼近阈值——**只跑默认态会全绿放行**。②**references D 节新增 D0「无读图能力时的机器代偿路径」**：旧版此门只有一句「无读图能力报 `skipped`」，等于给 D 门留了个黑洞——**越是弱模型/无多模态环境越容易整门跳过**，而 D 恰是唯一能抓「A/B/C 全绿但文本说错了」的门。D0 给出四条可代偿断言（①元素真渲染 ②可辨性 `ΔL`/`rel` ③层级正确 `elementFromPoint` 落点守卫 ④结构完整性）＋**明说代偿不了的**（好不好看/版式失衡/误读设计意图——代偿后仍须声明「主观视觉未复核」）＋四步纪律（先 `--selftest` 证检查器可信／缩放强制 100%／判据从产物推导禁魔数／留痕必须比「人看了一眼」更硬）。③**状态词四态 → 五态**：新增 `partial`（**仅 D 门机器代偿用**，禁止在 A/B/C/E 门使用，禁止把 `partial` 写回 `passed`）。④**references E 节拆 E1/E2**：**E1 逐变体**（主题/风格/Tab/视图切换后各自量一遍；`click` 后必须等过渡走完，否则采到中间态；控件遍历须回退基线验证「切换可回退」）＋**不锁方向的可辨性判据**（`|ΔL| ≥ minAbsL` **或** `rel ≥ minRel` 任一满足即过——深色风格靠 rel、浅色风格靠 ΔL，锁死单方向会把好产物判死）；**E2 纵向放行条件**（看板/报告页/单屏工具的纵向溢出是硬约束；**效果图/文化墙/展板/长页的纵向滚动是设计意图 → 须显式声明放行**，而**横向永远不允许放行**——横向溢出只可能是缺陷）。⑤**SKILL.md**：frontmatter 2.5.0 → **2.6.0**（description 同步改为五道门 ＋ 代偿说明）、Step 5.5 标题与表格补 E 门行与 E 门新增说明、状态词补 `partial`、参考资料与「模板文件与脚本」补外置工具指针（`ray-doc-toolbox` 的 `assets/probe_template.js` / `assets/struct_check_html.py`——**避免本 skill 重复造轮子**）。⑥**分工原则（三分法）**：**同一套流程横跨多处资产时不硬塞单 skill**——工序与工具落 `ray-doc-toolbox`，验收判据门落本 skill，领域内容落专家文档。 |
| 2026-09-17 | v2.5.0：CHEF 化学品库扩容的**回流审计修补（G8–G12）**——①**G8 新增坑点 9「数据提取层的静默损坏」**：提取代码用**全局 `replace` 剔小节号**，而小节号串会出现在**数据值内部**（`"69.209".replace("9.20"," ")` → `"6 . 9"`），一数被拆两数并使后续"对半切分"吞掉末值 → **整条曲线错位、不报错、退出码 0**；同族子缺陷＝**边界判据用形状而非结构**（`^\d+\.\d+$` 误匹配数据值 `0.487`，表格被提前截断）。纠正四条：行首锚点替代全局替换／优先行级解析／加结构闸门（等步递增·单调性）／边界用结构判别式；②**G9 新增坑点 10「扩充存量表——身份键去重＋拿存量惯例当合规尺」**：去重仲裁者须用**物理不变量**（同身份键而物理量矛盾 ⇒ 身份键错配，整组剔除），两条死路已记（用匹配名判会被同分异构体撞车误伤；用同批记录副本的索引表判则无仲裁力）；合规尺须**先测存量各字段填充率**——存量 10 个系数列 0% 空而 UFL 87% 空、自燃温度 89% 空，故新行留空**与存量同形、无需改引擎**；③**G10 Step 5 补「基准可信性 ＋ 归因三条」**：拿存量当基准前先证基准可信（实证某存量表液体密度系数差 10 倍）／**双源互证定责**（两独立源一致而与基准不符 ⇒ 基准错）／**"换参数无效"判据**（多参数重跑量级不变 ⇒ 差异来自数据源沿革而非实现，**不返工实现**）；④**G11 修陈旧索引**：SKILL.md 底部 pitfall 清单原把坑点 7 写作「导出与中文乱码」、缺坑点 8，与 `pitfalls.md` 实际（坑点 7 检查器自身缺陷／坑点 8 验证三类假信号）**失同步**，已改为实扫口径并补 9/10；⑤**G12 描述口径统一**：frontmatter description 原写"三层验证"（漏 D 门人工感知），改为**四道门**，与 Step 5.5 唯一规范对齐 |
| 2026-09-16 | v2.4.1：**内容待考（占位行）**——本行原本缺失（版本号已达 2.4.1 而无对应记录，属失同步）。据文件 mtime 证据 `2026-09-16 08:50:57` 推定为一次未记录的修订，**未臆造其变更内容**（处置先例同 `ray-translate` v3.6.3 占位行） |
| 2026-09-16 | v2.4.0：CHEF 4.5 承接方复核轮的**回流审计修补（G1–G5 + 新 G7）**——①**G1 门字母统一**：Step 5.5 定为唯一规范（A 确定性／B 离线自包含／C 运行时行为／D 人工感知），`references/verification-toolkit.md` 回执模板同步（原模板 B 误作「运行时行为」、C 误作「感知评审」、**整道 B 门缺失**），其原 D 节「COM 对拍台」**去编号**除撞字母；②**G2 哈希持久化**：证据绑定由"报告必须记录"加严为"**必须写进交付件/交接件本身**"（只写在会话报告里，下一轮承接方拿不到）；③**G3 D 门配方 + 假信号**：新增 references D 节（headless 截图配方 + **DOM 探针配对铁律**——截图不捕获 JS 面板，单看截图会误判白屏）；④**G4** `references/pitfalls.md` 坑点 6 补硬约束修正（零外部资源下"外部图片"方案仅限非单文件交付）；⑤**G5** 新增坑点 7「检查器自身的缺陷」；⑥**G7 溯源 Footer 陈旧**（Step 4 新增判据：Footer 阶段名/点位数须随阶段重建同步——CHEF 实证 P1~P4 四个产物 Footer 全停在「P1 · 25 842 点位」）；⑦未执行的门须显式报 `skipped`；新增坑点 8 收录本轮两例假信号 |
| 2026-09-15 | v2.3.0：外部对比（独立 HTML 产物技能的交付契约 / 离线自包含断言）+ CHEF 4.5 实战——**修掉"可离线使用"与"CDN 依赖"的矛盾**（改零外部资源 + Step 6 断言 + `assets/check_offline.mjs`）；新增 Step 5.5 三层声明分离 + 四态状态词 + SHA-256 证据绑定 + 修正轮次上限；新增 Step 7 运行时冒烟（jsdom）；新增 COM 对拍台规范（`CalculateFull` 卡死配方 / 精确 PID）；减法：删 CDN 表述与动效章节；细节下沉 `references/verification-toolkit.md` |
| 2026-09-02 | v2.2.0：外部对比（工程计算脚本审查类外部候选）——Step 5 补「源头验证层」：HTML vs Excel 一致 ≠ 正确（可能同源错误），关键公式须对照规范原文（HG/T 20570/GB 条款）核实形式与系数；仅凭 Excel 一致不能排除"两个都错" |
| 2026-08-29 | v2.1.0：外部对比（excel-to-html-dashboard / spreadsheets）——Step 5 升级三态验证门（✅交付/⚠️带缺口/⛔不交付）+ 浮点显示格式化规范；源校验补数据保真 2 禁止项；Step 0.5 前置结构勘查；溯源 Footer 规范 |
| 2026-08-12 | v2.0.0：分层卸载——UI 可视化设计规范/管道压力降设计规范/实时计算模块/通用 UI 规范/导出功能模板 → references/templates.md；坑点 1-7 → references/pitfalls.md。SKILL.md 1488→约 280 行，健康分 34→85+。功能不变，模板按需加载 |

| 2026-09-16 | v2.4.1：体积收敛——本表迁至 references/changelog.md；「常见陷阱与修复」陷阱 1-4 ＋ 经验总结检查清单迁至 references/pitfalls.md「泵系统公式对齐坑点」。SKILL.md 23,832 → ≈18,6xx B（est ≤8,000），流程与能力不变，正文对应位置留一行锚点 |
