---
name: ray-household-finance
slug: ray-household-finance
displayName: 家庭财务管理
version: "2.8.0"
description: "家庭财务通用管理框架，数据源无关（Excel/截图OCR/手动录入/API）。双契约：持仓快照 JSON（现状）+ 交易流水 transactions（历史，append-only）→ 成本基准/真实收益率/XIRR（scripts/cost_basis.py）。核心分析：持仓解析（含天天基金 Sheet3 定投计划表）、持仓对比、货基轮动（判据已改为结构性理由：费率/规模稳定性/快赎额度；收益率排名经回测证伪不作判据）、债基加仓（有锁定期产品禁做高频定投）、比例控制模型（±5%漂移带）、3年预测、可视化仪表盘。任意输入经数据源适配器归一化为标准 JSON，下游分析只认 JSON。可扩展：收支记账、预算、保险、储蓄目标、资产负债全景。触发词：基金持仓、持仓分析、投资组合、交易流水、成本、收益率、IRR、XIRR、家庭财务、收支记账、记账、预算、花了多少、活期宝轮动、货基轮换、债基加仓、定投计划、比例控制、资产配置、3年预测、资产规模预测、投资仪表盘、加息影响、宏观传导、事件冲击、Financial。"
agent_created: true
---

# ray-household-finance — 家庭财务管理框架

## Overview

家庭财务的通用管理框架，**当前以投资组合分析为核心、面向家庭财务全场景扩展**。

**数据源无关**——无论输入是天天基金 Excel 导出、手机截图 OCR、手动录入还是未来接入的 API，都先经「数据源适配器」归一化为**标准化持仓 JSON**，下游分析（分类/对比/轮动/比例控制/预测/可视化）只认这个 JSON，与输入源完全解耦。

**双契约（v2.2 起）**：①**持仓快照 JSON**（现状，存量资金有多少、结构如何）；②**交易流水 transactions**（历史，append-only 记买入/卖出/分红）。快照回答「现在怎么样」，流水回答「从头到尾真实赚了多少」——**真实收益率/XIRR 只有流水才能算**，快照里的 `gpct` 只是阶段收益。无流水时全部场景照常工作（流水是增强项，非前置依赖）。

核心流水线：**数据源适配 → 标准化 JSON → 对比快照 → 按场景分析 → 生成 HTML → 验证 → 交付**。

**定位演进**：本 skill 由「个人基金投资组合分析」演进为「家庭财务通用管理」，未来按需扩展收支记账、预算、保险、储蓄目标、资产负债全景——新场景沿用同一「数据源适配 + 标准化 JSON」架构，增量接入。已规划的扩展：收支场景引入「财务健康评分」四维度（储蓄率/必要支出占比/消费波动/应急储备 3-6 个月），待收支数据源接入后落地。**评分量化阈值参考（v2.4 补，外部同类校准）**：储蓄率 20%+ 及格 / 40%+ 优秀；负债收入比 <36% 健康 / 36-50% 警告 / >50% 危险；应急储备 3-6 个月月支出。

**收支记账方法论（v2.3 新增，收支场景落地时套用）**——记账是家庭财务第二常见场景，外部方法论已验证的核心规则（克制吸收，不重写架构）：

| 规则 | 内容 |
|------|------|
| **科目先粗后细** | 一级科目 ≤8 个（住/食/行/通讯/日用/社交/医疗/其他），不一开始就建 30+ 细目（记不住必弃坑） |
| **三字段起步** | 只记支出流水也够用：金额 + 科目 + 一句话备注，不追求每笔都记全 |
| **习惯二选一** | 每晚固定补记 或 支付后随手记，二选一固定下来（习惯 > 工具） |
| **月度复盘** | 按科目汇总 → 对比上月 → 找占比最大 + 涨幅最大的两类 |
| **预算只设 Top3** | 只给支出占比前三的科目设预算，其他放开不管（防预算疲劳） |
| **输出按数据量分级（v2.4 补）** | ≤3 条只给简短确认（关键数字加粗）；3-10 条给分类汇总表；>10 条才出完整分析报告（含趋势/环比/建议）——数据量撑不起报告时不硬凑 |
| **占比阈值预警（v2.4 补）** | 月度复盘时按科目占比给规则化提示（如餐饮 >40% 提示关注外卖频率、娱乐 >20% 提示冲动消费、结余 <10% 建议设储蓄目标）；阈值是参考线不是指令，结合当月实情说 |

落地方式：沿用数据源适配架构，新增 `manual`（口述记账）或 `excel`（账单导出）adapter 产出标准流水 JSON → 复用月度复盘/预算分析。触发词「记账/收支/预算」进入该场景。

## 标准化持仓 JSON（全流程的唯一契约）

所有适配器的产出、所有分析步骤的输入，都遵循同一 schema（完整字段定义见 `references/json-schema.md`，交易流水 schema 亦在该文件）。核心字段：`acls` 四类 `bond|index|qdii|mixed`；四类资产口径 = 活期宝/国内债基/美债QDII/ETF权益（指数型+纳指QDII 归 ETF 权益）；`yield_rate`/`gpct_s` 是字符串（可能含 `--`）。**任何新数据源，只做「把原始数据映射成这个 JSON」这一件事，下游零改动。**

## 数据源适配层（新增数据源的唯一接入点）

| 数据源 | 适配方式 | 现状 |
|--------|---------|------|
| **excel**（天天基金导出） | `scripts/adapters/excel.py` 位置解析 | ✅ 已实现 |
| **screenshot**（手机截图） | OCR（pytesseract/tesseract.js）→ 提取持仓文本 → 映射 JSON | 🧩 待接入 |
| **manual**（手动录入） | 对话中直接口述持仓 → 构造 JSON | 🧩 待接入 |
| **api**（未来开放接口） | 调 API → 映射 JSON | 🔮 规划中 |

新增数据源时：写一个 adapter 产出标准 JSON，其余六步流程**完全复用**。

## 场景路由（先识别场景，再走对应流程）

| 场景 | 触发词 | 数据/产出 | 核心逻辑 |
|------|--------|----------|---------|
| **adapt**（数据源适配） | 最新持仓、Financial-MMDD.xlsx、截图、持仓数据 | 任意源 → 标准化 JSON（**另读 Sheet3 = 定投计划表**） | `scripts/adapters/excel.py` |
| **compare**（持仓对比） | 对比、变化、diff、执行情况 | 本次 vs 上次快照 | 类别增减 + 建议执行核对 |
| **rotate**（活期宝轮动） | 轮动、货基、7日年化 | 非零持仓货基 + **费率表** | **结构性理由**（费率 / 规模连续净赎回 / 逼近清盘线 / 快赎额度）；**收益率排名不作判据**（2026-09-13 回测证伪） |
| **monitor**（定投周检，v2.8 新增） | 周检、宽基监控、黄金回本、PE 分位巡检 | 宽基/黄金监控 JSON + 看板 HTML | 三段式（数据 JSON + 生成脚本 + 周日自动化）；**触发前必过闸门**；提示词禁写死动态数值 |
| **bond**（债基加仓） | 债基、加仓、目标多少 | 债基当前→目标缺口 | **按久期四型分层**（中短债·滚动持有·长期纯债·二级债基）；有锁定期产品禁做高频定投 |
| **shock**（事件冲击响应，v2.7 新增） | 加息/降息、汇率、政策变动对我的持仓有何影响 | 暴露面分层 + 1σ/久期量级 + 机会成本 → 三层结论 | **算暴露 → 折量级 → 找"不能投的" → 分层穿透 → 对照定优先级** |
| **ratio**（比例控制） | 比例控制、资产配置、目标比例 | 四类资产目标比例 → 交互式 HTML | `scripts/ratio_control.py`（±5% 漂移带） |
| **cost**（成本基准） | 交易流水、成本、真实收益率、IRR、XIRR | 流水 JSON + 快照 → 成本/损益表 | `scripts/cost_basis.py` |
| **project**（3年预测） | 3年预测、资产规模、未来 | 36月堆叠曲线 HTML | 比例模型 + 每月充值 |
| **dashboard**（可视化） | 可视化、仪表盘、HTML | 全量持仓 HTML | 细化到每只基金 |
| **ledger**（收支记账，v2.3 新增） | 记账、收支、预算、花了多少 | 口述/账单 → 流水 JSON | 科目≤8 + 三字段 + 月度复盘 + Top3 预算 |

## 通用执行流程（六步）

### 第 1 步：数据源适配（必须先做，不能跳过）

将任意输入归一化为标准 JSON。Excel 源：`scripts/adapters/excel.py <excel路径> <日期>` → 默认写 `~/.workbuddy/data/ray-household-finance/_portfolio_data.json`（v2.4.1 起默认输出**技能包外**数据目录，第 3 参数可覆盖——家庭财务敏感数据不进技能包）。

⚠️ **Excel 结构铁律**（仅 Excel 适配器）：天天基金 Excel 是**纯位置解析**（非标准表格），必须先 `load_workbook` 打印 `sheetnames` + 前 6 行原始值，核对后再写解析逻辑。活期宝 sheet 行结构：`名称 | 7日年化 | 可用/可取/总份额 | 未付收益 | 累计收益`；基金 sheet 每只基金占 6-7 行块（名称行 + 类型行 + 金额行 + 状态行 + 盈亏行 + 收益率行），**状态值含「有在途交易/到期/未到期/将到期」四种**，块高随状态变化（6 或 7 行），必须用「名称行含 6 位代码」作为锚点 + 状态值判断动态跳行。

### 第 2 步：资产分类

- 债券型 → `bond`；指数型 → `index`；QDII → `qdii`；混合型 → `mixed`
- 四类资产口径：**活期宝 / 国内债基 / 美债QDII / ETF权益**（指数型+纳指QDII 归入 ETF 权益）
- ⭐ **穿透校验（v2.7 新增）**：`type_info` 的分类标签**不等于实际风险**——「债券型」里混着**二级债基 / 可转债基（含权益）**。分类后必须回查**实际资产占比**（股票 %），不可只看标签。实证：110027 标签为债券型、`acls=bond`，实际股票 **19.72%**，波动率 **9.16%** vs 纯债 0.61% = **15 倍**；近 1 月 −1.21% 而同期纯债 +0.26%。
- 提取后立即打印核对：**部分之和 == 总资产**，验证通过才继续。

### 第 3 步：对比快照

读项目 `MEMORY.md`（或上次快照，默认 `~/.workbuddy/data/ray-household-finance/_portfolio_data.json`）取上次数字，做类别增减表 + 上次建议执行核对表（✅已执行/🟡部分/⏳未执行）。

### 第 4 步：按场景分析

- **rotate**：**不按收益率排序选品种**。先查**结构性理由**（费率合计 / 规模连续净赎回 / 逼近清盘线 / 快赎与到账规则），再比**机会成本**（大类配置缺口 vs 同类内轮换，通常差一个数量级）——见下方「货基轮动判据」节
- **bond**：先按**久期四型**分层（中短债·短债 / 滚动持有 / 长期纯债 / 二级债基），再算当前→目标→加仓额。**搬家优先中短债 A 类（久期短、吃票息、最不惧利率上行）**；长久期纯债与二级债基**不因"收益高"而选**。
  - 四型久期假设（评估敏感度用，非实测）：中短债/短债 **1.0 年**｜滚动持有 **1.0 年**｜长期纯债 **3.5 年**｜二级债基债券部分 **1.0 年**（权益部分另算）。
- **ratio/project**：见下方「比例控制模型」

### 第 5 步：生成 HTML（可视化）

**模板注入模式**：Python 脚本生成 HTML，图表数据**必须由脚本注入**（`json.dumps` 或 f-string），**绝不手写数据数组**（手写必抄错，已踩坑）。

### 第 6 步：验证 + 交付

**A. 数据验证清单（交付前逐项核对）**：

- [ ] **部分之和 == 总资产**：`mm_total + fund_total == grand_total`；`class_totals` 各键求和 == grand_total
- [ ] **四类和 == total**（比例/预测场景）：模拟输出四类资产之和恒等于总资产，逐月校验
- [ ] **权重和 = 100%**：四类占比（或持仓明细权重）加总为 100%（允许 cash 项）
- [ ] **数据源标注**：输出开头标注数据来源与快照日期（如「数据：Financial-0809.xlsx，净值日期 08-07」）
- [ ] **stale data 警告**：若净值日期距快照日期 >3 天，或收益率明显异常（如货基年化 >5% 或 <0.3%），显式提示数据可能滞后
- [ ] **`\ufffd` 扫描**：`html.count('\ufffd')` 必须为 0（零容忍）
- [ ] **金额转"万"用 `toFixed(1)`**：`(x/10000).toFixed(1)`，不要 `Math.round(x/10000)/100`

**B. 集中度检查**：

- 单只基金金额 > 总资产 10% → 标记 ⚠️ 集中度风险
- 债基双核心（嘉实汇鑫/长安泓沣）合计占国内债基比，若 >60% 需提示分散

**C. 输出结构模板**（固定 7 段，不遗漏）：

1. **快照总览**（类别 × 金额 × 占比表）
2. **对比变化**（本次 vs 上次，类别增减表）
3. **执行核对**（上次建议 ✅已执行/🟡部分/⏳未执行）
4. **场景分析**（rotate/bond/ratio 按触发场景）
5. **集中度提示**（如有 >10% 或 >60% 集中，单独列出）
6. **行动清单**（P0/P1/P2 分级，具体到金额和基金）
7. **结论信号**（组合状态 + 下一步核心动作 + 置信度，见下）

**D. 结论信号框**（结尾固定输出）：

```
┌─ 投资结论 ─────────────────┐
│ 状态: 稳健 / 需调整 / 偏谨慎 │
│ 下一步: [核心动作一句话]      │
│ 置信度: 高 / 中 / 低         │
└────────────────────────────┘
```

- `present_files` 打开预览；回复给核心结论表 + 行动清单（P0/P1/P2）。

## 比例控制模型（核心方法论，2026-08-05 确立）

用户已采纳的资产配置策略（替代固定定投）：

- **目标比例（方案A）**：活期宝 30% / 国内债基 50% / 美债QDII 12% / ETF权益 8%
  - ⚠️ **加权年化三态分列，勿混**（v2.7 订正）：**现行目标 2.8%** ｜ 测算 **2.81%**（配置到位 2.56% ＋ 债基提质 0.25pp）｜ 旧记 **2.46% 已过期**，不得再引
- **机制**：每月定额充入活期宝 → 计算四类目标额 = 总资产 × 目标比例 → 活期宝超配部分按缺口比例流向低配类
- **漂移带（v2.2 新增，±5%）**：某类资产实际占比偏离目标 < 5 个百分点时**当月不调仓**（带内不动），超带才调——避免月月微调、减少无意义操作。`scripts/ratio_control.py --band 5`（默认 5，`--band 0` 恢复逐月精确调仓）
- **约束**：① 月转出上限（默认约 5 万）② QDII 限购约 1 万/月（超额转债基）
- **收敛**：活期宝首月 67.4% → 约第12月<40% → 第15月收敛到 30%；3年后达到目标资产规模
- **模型代码**：`scripts/ratio_control.py`（模拟 36 月，输出每月余额 + 建议转出额）

## 货基轮动判据：结构性理由，不看收益率排名（v2.6 修订，2026-09-13 回测证伪）

> ⛔ **v2.5 的「双边合格线（收益率判据）」已于 2026-09-13 被回测正式证伪、整节废止**——旧内容降级为下方「作废判据」区**仅作教训留档，不得再据此下单**。
>
> **证伪证据**（近 1 年 20 只货基，真实万份收益逐日复利；近 3 年 18 只同结论）：

| 策略 | 年化 | 说明 |
|------|------|------|
| **等权持有全部（基准）** | **1.4741%** | 什么都不做 |
| 起点买入「当时最优」并持有 | 1.3774% | — |
| 每周追「4 周最优」· 无摩擦 | 1.3618% | 光换手就亏 |
| 每周追「4 周最优」· 2 天空档 | **1.1155%** | 扣摩擦后 |
| 每周追「4 周**最差**」· 无摩擦（反向对照） | 1.2304% | **追最优竟跑输追最差** |

- **追排名是负 alpha**：当期第 1 名 3 个月后**中位落到第 9.0/20 名**，进前 3 命中率 **12% ＜ 随机 15%**；但当期 → 未来 3 月秩相关 **+0.461** ⇒ **排名中段有持续性、顶端反转，而轮动恰只抓顶端**。
- **摩擦成本与理论收益同量级**：作用于全部活期宝 → 1 天空档 **−¥1,325/年**、2 天空档 **−¥2,649/年**，而横截面收益极差仅 0.3080pp。
- **差距的一半是费率，不是能力**：费前收益极差 **0.1590pp ≈ 费率极差 0.1600pp** ⇒ **费率占到手差距 52%**；费率 ↔ 到手收益 **−0.737**。

### ✅ 现行判据（按可靠性排序，v2.6 生效）

| 优先级 | 判据 | 为什么 | 阈值 / 动作 |
|--------|------|--------|-------------|
| 1️⃣ | **费率合计** | **永久确定**——今天查得到、明天不会变 | 区间 0.20%~0.36%；高费率者优先轮出，目标 ≤0.21% |
| 2️⃣ | **规模稳定性** | **唯一"必须换"的结构性理由** | 连续净赎回 / 逼近清盘线（<1.5 亿）→ 必须搬 |
| 3️⃣ | **快赎额度与到账规则** | 影响实际可用性 | 单只单日快赎 ¥1 万；¥1 万以上走普通赎回 T+1 |
| ❌ | ~~收益率排名 / 1.25% 合格线~~ | **波动的差异，追不到** | 仅作提示信息，**不触发交易** |

**配套纪律**：① 轮动候选排序以**非零持仓**为基础（`shares > 1`），零残渣基金不参与比较；② **动手前先比机会成本**——同类别内轮换的增益通常比大类配置纠偏低一个数量级以上（实测 ¥166/年 vs ¥3,032/年 = **18.3 倍**），先补齐大类缺口；③ 数据字段：货基 7日年化在标准 JSON 的 `yield_rate`，形如 `"0.3336（1.2420%）"`——括号内是 7日年化，括号外是万份收益。

---

### 📕 作废判据（v2.5「双边合格线」· 仅作教训留档，禁止据此下单）

> ⛔ **作废原因**：收益率排名的预测力在**顶端反转**（当期第一 → 3 月后中位第 9 名），而轮动只抓顶端；且扣摩擦后跑输等权基准、甚至跑输反向对照。**费率与规模才是可依据的判据。**
> ⚠️ 唯独「**7日年化 > 2% 先观望一周**」**仍有价值**——异常高收益多是一次性收益尖峰（实证：某货基 5 个交易日从 1.4070% 掉到 1.2010%），保留为**风险提示**而非轮动依据。

> **为什么需要**：轮动策略最常犯的两个错——只设轮出线不设轮入线（低换低）、把异常高收益当机会（追高踩坑）。两条都是**真金白银的实战损失**，故升为硬判据。

| 方向 | 判据 | 说明 |
|------|------|------|
| 🔴 **轮出** | 金额 **≥ ¥10K** 且 7日年化 **< 1.25%** | 两个条件**同时**满足才动；小额（<¥10K）即使收益低也不值当操作，摩擦成本吃掉收益 |
| 🟢 **轮入** | 7日年化 **≥ 1.25%** | **轮入也须设合格线**——低收益换低收益 = 白折腾 |
| ⚠️ **异常** | 7日年化 **> 2%** → 视为异常，**先观望一周**再决定 | 货基正常中枢 1.2-1.4%；突然冒 2%+ 大概率是促销/冲量/短期扰动 |

**两条实证教训（不要再犯）**：

1. **追异常高收益**：某货基以 **2.524%** 虚高收益被追买 ¥31K，一周后崩至 **1.034%**（全场最低），大仓站岗。→ 异常收益率**先观望一周**，不追。
2. **轮入无合格线**：某货基 ¥30.7K 轮入时仅 **1.112%**（低于轮出线），低收益换低收益。→ 轮入同样适用 **≥1.25%**。

**配套**：轮动候选排序一律以**非零持仓**为基础（`shares > 1`），零残渣基金不参与比较（否则"Top"里混进空壳）。
> 数据字段：货基的 7日年化在标准 JSON 的 `yield_rate` 字段，形如 `"0.3336（1.2420%）"`——**取括号内的 7日年化**，括号外是万份收益。

## 定投与锁定期纪律（v2.5 新增，2026-09-05 实证）

**核心规则：有锁定期的产品，禁止做高频（日/周）定投。**

| 项 | 说明 |
|----|------|
| **为什么** | 滚动持有型产品（如"60天滚动"）每笔份额**各自**锁 N 天 → 日定投会形成"每天都有一笔在锁定期"的**碎片化流动性**，赎回时既算不清可动用额度，又容易误判"随时能取" |
| **正确做法** | 债基搬家聚焦**无锁定**品种（中短债 A 类）作为双核心；有锁定期产品只做**一次性配置**、不做定投 |
| **易误判点** | **停止定投 ≠ 解除既有份额的锁定**——停掉日定投只是不再新增锁定份额，存量各笔仍按各自买入日计锁定期、须逐笔等到期。对外说明时必须讲清，否则用户会以为"已可随时赎回" |

**实证（2026-09-05）**：用户自行为"中加聚安60天滚动"开 **¥500/日** 定投，想法是"日定投形成滚动流动性"；当晚想清楚锁定期摩擦后**自行停止**。→ 原判断"滚动流动性是优点"**被推翻**：事实是每笔各锁 60 天、流动性碎片化。
> 这条也是**正面案例**：用户试错快、纠错快——但 AI 的职责是在开定投**之前**就提示锁定期摩擦，而不是等用户自己发现。

## 定投计划表解析（v2.5 新增：天天基金 Sheet3）

天天基金导出的 Excel **第三个 sheet = 定投计划全表**（`Sheet3`，早期导出可能无此表）。这是"用户当前在扣哪些款"的**权威来源**——比逐个询问快得多。

**表结构（非标准表格，须按锚点解析）**：

| 元素 | 内容 |
|------|------|
| **计划头行** | 基金代码 / 基金名称 / 每期金额 / 周期（每日·交易日、每周·周X、双周·周X）/ 扣款日 / 下次扣款日 / 状态（正常、暂停扣款、已暂停…） |
| **附属行** | 该计划绑定的**银行卡**信息（紧随计划头之下，无基金信息） |
| **常见坑** | ①**代码常省略前导 0**（`7194` → 须补为 `007194`）②同一基金可能**新旧两条并存**（旧条暂停 + 新条正常）→ 视为重复，建议清理 ③状态字段是**唯一**判断"运行/暂停"的依据，不要靠"下次扣款日"推断 |

**输出建议形态**：结构化 JSON（每条含 `name/code/cls/amount/freq/next/run/act/reason`），`act` 为处置建议枚举（`keep` 保留 / `rm` 清理 / `pause` 建议暂停 / `hold` 保持暂停观察 / `keep_pause` 暂停待启用条件单），供仪表盘分页展示。

**已见模式**：无对应持仓的暂停计划（清理或挂观察）、同质叠加（AI + 云计算）、宽基条件单（暂停但**勿删**，等 PE 分位触发启用）。



## 交易流水与成本基准（v2.2 新增）

**schema**：`transactions.json`（append-only，完整定义见 `references/json-schema.md`）——每笔交易含 `date / fund_code / fund_name / type(buy|sell|dividend) / amount / nav / fee`。只追加、不修改；从启用日起记录，历史无法回补，成本基准以流水起点为准。

**计算**（`scripts/cost_basis.py <transactions.json> [snapshot.json]`，口径均为金额口径为主、份额法为辅）：

- **累计投入** = Σ buy；**累计回收** = Σ sell + Σ dividend
- **累计损益** = 期末现值 + 累计回收 − 累计投入；**真实收益率** = 累计损益 / 累计投入
- **加权平均成本** = Σ买入净额 / Σ买入份额（仅统计带 `nav` 的买入）
- **XIRR**：每笔买入为负现金流、卖出/分红记为正、期末现值为最后一笔，解年化内部收益率

**输出必带两条诚实声明**：①成本基准从流水起点算起，不含启用前的历史交易，故非「完整历史收益率」；②快照中未记流水的基金会缺失现值，脚本会显式提示（属正常，不是 bug）。

## 诚实声明（每次分析必带）

输出任何分析/预测/建议时，必须包含以下三条（可直接放在结论信号框下或文末）：

1. **这是条件规划，不是指令**：比例模型/3年预测的所有数字都是「给定假设下的条件测算」，不是交易指令；实际操作需用户确认。
2. **收益是概率估算，非承诺**：债基 2.5%/QDII 3.5%/ETF 5% 均为历史均值假设，不构成收益承诺；市场波动会使实际偏离。
3. **降低持仓 ≠ 必然增值**：轮动/搬家优化的是结构（提升预期收益、分散风险），不是保证每笔都赚钱；久期长、汇率波动都可能造成短期回撤。

## 失败模式编码（thesis-break gate）

明确「什么情况该暂停/停止」，而非盲目执行：

| 触发条件 | 应对动作 |
|---------|---------|
| 🔴 债市持续下跌（10Y 收益率 >2.0%） | 暂停债基新增买入，已持仓不动，等企稳再续 |
| 🔴 人民币大幅升值（USD/CNY <6.5） | 暂停美债 QDII 追加，现有仓位暂持 |
| 🟡 活期宝跌破安全线（<安全阈值 或 <25%） | 暂停搬家，优先补充活期宝流动性 |
| 🟡 QDII 长期限购（连续数月买不进） | 接受 QDII 低配，超额转债基，不强求达标 |
| 🟡 某只基金单日限购（极低限额） | 放弃该基金做主力，缺口转给可正常买入的同类 |

**每次按比例模型执行前，先检查上表是否触发**；触发即调整节奏，不机械照搬模型输出。

### 闸门数据源与自洽校验（v2.8 固化，2026-09-21 实战验证）

闸门检验必须**独立 HTTP 源 + 三源交叉 + 自洽校验**，不依赖 MCP 工具是否暴露到会话：

| 闸门 | 主源 | 交叉源 | 自洽校验 |
|------|------|--------|---------|
| 🔴 10Y 中债 >2.0% | 东财 datacenter `RPTA_WEB_TREASURYYIELD`（取 `y10`） | akshare `bond_zh_us_rate`（同源，用于口径核对） | `y10 − y2 == spread` 字段（±0.5bp），全样本断言 |
| 🔴 USD/CNY <6.5 | 中国货币网 `ccpr.json`（PBOC 授权中间价） | 新浪 `hq.sinajs.cn` 现货 + `frankfurter.app`（ECB） | 两源差 >0.5% 即如实标注，不静默取一 |

**配套纪律**：① 区间统计防「pageSize 陷阱」——`RPTA_WEB_TREASURYYIELD` 默认 pageSize=60，会把「近60日」误当「2026年内」（实测区间宽 6.69bp vs 真值 22.25bp）；**要年区间必须 pageSize≥300 并双口径分列**。② 东财日期为非交易日时**顺延取最近披露日**并在输出标注 `non_trading_day_carry`。③ 闸门结果必须落盘 JSON（含 `dist_gate_bp` 距离字段），作为触发有效性的证据文件。

## 定投周检监控（monitor 场景，v2.8 新增）

**触发词**：周检、宽基监控、黄金回本、PE 分位巡检

**三段式架构**（已验证稳定运行）：数据 JSON（`_wide_index_data.json` / `_gold_monitor_data.json`）+ 生成脚本（`_build_wide_monitor.py`）+ 每周日 20:00 自动化巡检（AI 承担 WebSearch 分位/金价更新）。

| 项 | 判据 |
|----|------|
| 宽基触发 | PE 十年分位：上证50 <60% / 沪深300 <70% / 中证500 <50%（触发前**必须先过闸门**，见上节） |
| 黄金定投档位 | 锚伦敦金五档（档位数值以数据 JSON 的 `dca.note` 为准，**提示词/报告不写死**——乘数与区间随持仓成本动态重算） |
| 黄金硬约束 | 总量 ≤ 总资产 1.5% ｜ 周投上限 ¥500 ｜ 不叠加慧定投 |
| 回本参考线 | 金价 × `gold_ref.break_even_mult`（乘数 = 成本净值口径，随最新持仓重算；**勿用历史写死值**） |

**自动化提示词纪律（2026-09-21 事故回流）**：巡检自动化提示词里**禁止写死**会随时间失效的数值（乘数/区间/持仓金额）——一律改为「读 JSON 派生字段」；且**闸门步骤必须显式写入提示词**（只写在 skill 文档里，自动化执行体会跳过——09-20 周检漏闸门的根因即此）。

## 金额与预算的派生纪律（v2.8 新增）

- **汇总数从明细项实时求和**，禁用手写总数：停掉一条定投线后，月投合计若沿用旧口径手写值就会多算（实证：010810 停扣 ¥100/日 后总数仍按 v5 口径写 ¥1,300/日）。判据与「数量类标注计算注入」同源——**凡会因增删条目而变的数字，一律 `sum()` 现算**。
- 同一数字多口径时**分母显式标注**：如「机会成本」有活期宝超配与债基缺口两个分母，引用前先确认分母。

## 共享红线（全场景通用）

1. **反引号坑（铁律）**：写含反引号(`` ` ``)的 Markdown 文件**必须用 .py 脚本文件方式**，禁止 bash -c 内联（反引号被 shell 解析成命令替换，会破坏文件内容、混入垃圾行）。已两次踩坑。
2. **图表数据脚本生成**：HTML 图表的数据数组绝不手写，一律脚本注入（手写曾把大额数字写错一个数量级，还出现过数组笔误导致曲线锯齿）。**推论（v2.5）**：**数量类文字标注也须由数据计算注入**（如表格标题的"N 条"用 `len(arr)`，不写死数字）——2026-09-05 实证：数据已从 25 条收敛到 17 条，标题仍写"25 条"，数据对而标注错，肉眼扫过不易发现。
3. **金额转"万"用 `toFixed(1)`**：`(x/10000).toFixed(1)`，不要 `Math.round(x/10000)/100`（多除 100 导致大额显示成小一个数量级）。
4. **Python 列表深拷贝**：模拟循环里 append 列表用 `bal[:]`，引用复用会导致下一轮覆盖、误报偏差。
5. **写完必扫 \ufffd**：零容忍，任何产出文件都扫描。
6. **用户画像固定**：Rayzhang，偏保守，核心诉求"稳"，目标年化 2.5%；货基每周轮动 Top5-6；操作风格少问多做、输出表格+emoji。
7. **隐私保护（v2.1 新增，v2.3 升级为脚本级，v2.4.1 加路径红线）**：处理的是家庭财务敏感数据（持仓金额、银行账号、收入）。四条强制：①**脚本级脱敏**——所有 `scripts/*.py` 输出账号/卡号前必须自动 mask（`****1234` 只留末 4 位，输出前统一过滤，不靠"记得手动脱敏"）；新增脚本/适配器时默认套用；②敏感数据**优先本地处理**，不主动上传第三方；③生成的 HTML/报告含金额时，默认仅本地预览，分享前先经用户确认；④**运行时数据文件不进技能包**——快照/流水 JSON 默认写 `~/.workbuddy/data/ray-household-finance/`（技能包外），发现包内出现数据 JSON 即为泄漏隐患，先移出再继续。
8. **`%` 转义坑（v2.7 新增，与红线 2 并列）**：Python 模板里 `%` 写 `%%` 是对 f-string/`%` 格式化的要求；**非格式化字符串里写 `%%` 会被原样渲染成 `%%`**（实证 2026-09-17：`_rate_bond_0917.py` 直出文本第 44–50 行全部 `%%` 双写，肉眼扫过不易发现，只在落盘回读时暴露）。→ 生成器加 `assert '%%' not in html` 自校验；**定位法 = AST 取 `Assign(Name('html'))` 的字面量逐字符扫**（正则法误报率高）。

## 资源结构

```
ray-household-finance/
├── SKILL.md                      # 本文件（框架 + 路由 + 红线 + 诚实声明 + 失败模式）
├── references/
│   └── json-schema.md            # 标准化 JSON + 交易流水 schema 完整定义（按需加载）
└── scripts/
    ├── adapters/
    │   └── excel.py              # 天天基金 Excel → 标准 JSON（当前唯一已实现）
    ├── extract_portfolio.py      # 别名入口（向后兼容，转发到 adapters/excel.py）
    ├── ratio_control.py          # 比例控制模型：36月模拟 + ±5%漂移带 + 建议转出额
    └── cost_basis.py             # 交易流水 → 成本基准 + 真实收益率 + XIRR（v2.2 新增）
```

## 实战经验

- 2026-09-21｜[LRN-20260921-001]｜场景：monitor + gate｜经验：**闸门与周检的流程缺口三件套回流**。① 09-20 周检漏做闸门检验，根因＝自动化提示词无该步骤（skill 文档里有但执行体看不到）→ 闸门步骤必须显式写入自动化提示词；② 东财国债接口 pageSize=60 会把近60日当全年（区间宽 6.69bp vs 真值 22.25bp）→ 年区间统计须 pageSize≥300 且双口径分列；③ 巡检提示词写死 ×1.261 乘数与 $4,400-4,500 区间（均已过期）→ 动态数值一律改读 JSON 派生字段。以上已固化为 v2.8「闸门数据源与自洽校验」+「monitor 场景」节｜死路：以为「skill 文档写了」自动化就会执行；用接口默认 pageSize 做年区间统计｜来源：09-21 self-neat（automation-1787489198215 重写）｜计数：1｜状态：resolved

- 2026-09-11｜场景：版本回流｜经验：**定投计划表解析规范（Sheet3）**+「定投与锁定期纪律」回流入包。① Sheet3 = 定投计划全表，是「当前在扣哪些款」的权威来源（`scripts/adapters/` 侧）：代码常省前导 0 须补全（`7194`→`007194`）、同基金新旧两条并存视为重复、**状态字段是判断运行/暂停的唯一依据**；② 有锁定期的产品**禁做高频定投**（"60 天滚动"型日定投会使每笔各锁 60 天、流动性碎片化），**停止定投 ≠ 解除既有份额锁定**；③ 货基轮动新增「双边合格线」判据（轮出 <1.25% 且 ≥¥10K、轮入 ≥1.25%、>2% 先观望一周）—— **⚠️ 其中③「双边合格线」已于 2026-09-13 被回测证伪，状态 superseded，详见下一条（v2.6.0）**｜死路：按"下次扣款日"推断状态、把滚动锁定当流动性优点｜来源：v2.4.1 → v2.5.0｜状态：partially superseded

- 2026-09-05｜场景：rotate｜教训：**货基轮动须设双边合格线**——①轮出 = 金额 ≥¥10K **且** 7日年化 <1.25%（两条件同时满足，小额不值当操作）②轮入 ≥1.25%（低换低 = 白折腾）③7日年化 >2% 视为异常**先观望一周**。两条实证：某货基 **2.524%** 虚高被追买 ¥31K、一周崩至 **1.034%** 全场最低；另一只 ¥30.7K 轮入时仅 **1.112%**｜死路：只设轮出线不设轮入线（会低换低）、把异常高收益当机会（会追高）｜来源：Financial-0905 轮动分析｜状态：resolved⚠️ 本条判据已于 2026-09-13 被回测证伪，**状态改为 superseded**（详见下条）

- 2026-09-13｜场景：rotate｜教训：**「按收益率轮动」被回测证伪，判据整节替换为结构性理由**（费率/规模/快赎）。回测（近 1 年 20 只，真实万份收益逐日复利）：等权持有 **1.4741%** ＞ 追「4 周最优」无摩擦 1.3618% ＞ 扣 2 天空档 **1.1155%**（**跑输反向对照"追最差"1.2304%**）；近 3 年同结论。抓「当期第一」是负 alpha：3 个月后中位第 **9.0/20** 名、进前 3 命中 **12% ＜ 随机 15%**；但当期 → 未来 3 月秩相关 **+0.461**（中段持续、顶端反转）。另一半真相：费前收益极差 **0.1590pp ≈ 费率极差 0.1600pp** ⇒ **费率占到手差距 52%**、费率 ↔ 到手 **−0.737**｜死路：按 7 日年化排序挑轮入品种（追排名 = 负 alpha）、只看收益率不看费率与规模｜来源：Financial-0912 活期宝收益率复核｜状态：resolved（判据已替换 → v2.6.0）

- 2026-09-05｜场景：bond｜教训：**有锁定期的产品禁止做高频定投**——"60天滚动"型日定投会使每笔份额各锁 60 天、流动性碎片化；债基搬家应聚焦**无锁定**中短债 A 类双核心。易误判点：**停止定投 ≠ 解除既有份额锁定**（存量仍按各买入日计锁定期）。实证：用户自开"中加聚安60天" ¥500/日后当晚想清摩擦自行停止，原判断"滚动流动性是优点"被推翻｜死路：用日定投制造"滚动流动性"（实为碎片化）｜来源：Financial-0905 定投体系讨论｜状态：resolved

- 2026-09-05｜场景：adapt｜经验：天天基金 Excel **Sheet3 = 定投计划全表**，是"当前在扣哪些款"的权威来源，比逐个询问快。格式=计划头（代码/名称/金额/周期/扣款日/下次/状态）+ 银行卡附属行；**代码常省略前导 0 须补全**（`7194`→`007194`）；同基金可能新旧两条并存（视为重复）；状态字段是判断运行/暂停的唯一依据｜死路：按"下次扣款日"推断状态｜来源：Financial-0905 Sheet3 解析（25 条 → 用户清理至 17 条）｜状态：resolved

- 2026-09-08｜场景：进化+隐私整改｜经验：外部对比吸收 2 项（财务健康评分量化阈值：储蓄率 20/40%、负债收入比 36/50%、应急金 3-6 月；记账输出按数据量分级 + 占比阈值预警）→ v2.4.0。**隐私整改**：进化轮隐私检查发现包内 `_portfolio_data.json` 含真实持仓（25 基金代码/大额金额）——excel.py 默认输出原写在脚本自身目录（技能包内），改为默认 `~/.workbuddy/data/ray-household-finance/`（第 3 参数可覆盖），真实数据文件迁出，共享红线④新增"运行时数据文件不进技能包" → v2.4.1｜来源：v2.3.0 → v2.4.1

- 2026-09-02｜场景：进化｜经验：外部对比吸收 2 项——①共享红线「隐私保护」从纯文字升级为**脚本级脱敏**（所有 scripts 输出账号自动 mask，不靠"记得手动脱敏"）；②新增「收支记账方法论」增量接入（科目≤8/三字段起步/习惯二选一/只给 Top3 设预算），作为收支场景落地默认方法论，沿用数据源适配架构不重写。克制吸收：跳过 beancount 复式记账（个人家庭场景过重）、跳过预算 vs actual 企业级 variance 分析（非家庭场景）｜来源：v2.2.0 → v2.3.0

- 2026-08-28｜场景：进化｜经验：外部对比吸收 2 项——①交易流水第二契约（transactions.json append-only + cost_basis.py：加权平均成本/真实收益率/XIRR），补上「快照只能看现状、算不了真实收益」的架构级差距；②比例控制模型加 ±5% 漂移带（带内不动、超带才调），避免月月微调。克制吸收：跳过 sparkline（已有时效标注）和 provider fallback（边际）｜来源：v2.1.1 → v2.2.0

- 2026-08-15｜场景：进化｜经验：对比外部 skill 吸收 2 项——①隐私保护红线（账号脱敏、敏感数据本地处理、分享前确认）；②财务健康评分四维度（储蓄率/必要支出/波动/应急储备）记为未来扩展规划，待收支数据源接入后落地｜来源：v2.0.0 → v2.1.0

- 2026-08-15｜场景：改名｜决策：ray-financial-analysis → **ray-household-finance**（家庭财务管理），定位从「个人基金投资组合分析」升级为「家庭财务通用管理」；当前核心是投资组合分析，未来增量扩展收支/预算/保险/储蓄/资产负债，沿用同一数据源适配架构｜来源：v1.2.0 → v2.0.0（改名+定位升级，major bump）
- 2026-08-15｜场景：进化｜经验：对比外部 skill 吸收 5 个机制——数据验证清单、输出结构模板(7段)、集中度检查(>10%)、诚实声明(3条)、失败模式编码(thesis-break gate)；JSON schema 卸载到 references/｜来源：v1.1.0 → v1.2.0
- 2026-08-15｜场景：架构｜经验：skill 从「Excel 工具」升级为「数据源无关的持仓分析平台」——引入「标准化持仓 JSON」作为唯一契约 + 「数据源适配层」隔离输入差异（excel/screenshot/manual/api），新增数据源只写 adapter、下游六步零改动｜来源：v1.0.0 → v1.1.0
- 2026-08-09｜场景：parse+compare｜教训：天天基金 Excel 基金 sheet 块高随状态值变化（有在途交易=6行/到期=7行），必须用"名称行含6位代码"锚点 + 状态值动态跳行，不能固定步长｜来源：Financial-0804/0809 解析
- 2026-08-05｜场景：ratio｜教训：比例模型首次跑出"第1月建议一次性大额转出"（违反分批定投原则）+ QDII 单月转出触发限购；加"月转出上限 + QDII 月限购"约束后渐进收敛｜来源：比例控制仪表盘开发
- 2026-08-05｜场景：dashboard｜教训：KPI 金额 `Math.round(x/10000)/100` 把大额显示成小一个数量级（多除100）；改 `(x/10000).toFixed(1)`｜来源：比例控制仪表盘
- 2026-08-05｜场景：dashboard｜教训：对话内嵌曲线手写数组笔误导致"波动上升"锯齿；HTML 脚本生成的数据一直正确，此后图表数据一律脚本注入｜来源：3年预测仪表盘
- 2026-08-05｜场景：bond｜教训：某中短债基金"年度收益高"是记忆偏差（近1年跑输同类），且单日限购极低无法加仓；久期长的中长期纯债债牛猛、震荡落后，不适合保守组合做主力｜来源：债基加仓讨论
- 2026-07-13｜场景：plan｜经验：年化目标计划三阶段（活期宝从高配逐步搬到国内债基+美债QDII）；用户风险偏好保守，原更高目标过于激进已下调｜来源：目标计划制定
- 2026-08-23｜场景：parse｜经验：天天基金导出 sheet 名可能变化（"活期宝/基金" → "Sheet1/Sheet2"），适配器已改按位置取 sheet（第一个=活期宝，第二个=基金），不再依赖 sheet 名｜死路：按名匹配（wb['活期宝'] 直接 KeyError）｜来源：Financial-0823 解析｜状态：resolved
- 2026-08-23｜场景：compare｜经验：旧快照 JSON 会被新数据覆盖，对比基线可从历史仪表盘 HTML 内嵌的 `const FUNDS = [...]` / `const MM = [...]` 用正则提取，无需重新解析旧 Excel｜死路：依赖 _portfolio_data.json 存历史（单文件被覆盖）｜来源：0815 vs 0823 对比｜状态：resolved
- 2026-08-23｜场景：monitor｜经验：指数估值数据源可用性——东财 push2 估值字段接口有风控（RemoteDisconnected）、乐咕乐股 405 需登录、腾讯 proxy.finance.qq.com 只给行情不给 PE；分位数据靠每周 AI WebSearch 更新，点位走腾讯接口（稳定）｜死路：东财字段探测（连发触发风控）｜来源：宽基监控工具开发｜状态：resolved
- 2026-08-23｜场景：monitor｜经验：条件触发型定投工具（宽基 PE 分位阈值 + 黄金回本线）可做成「数据 JSON + 生成脚本 + 每周日自动化巡检」三段式，AI 在自动化里承担 WebSearch 数据更新职责；看板数据一律脚本注入不手写｜死路：全自动 API 拉分位（免费接口拿不到）｜来源：宽基+黄金监控｜状态：resolved

- 2026-09-15｜[LRN-20260915-001]｜场景：diagnose（持仓诊断）｜经验：**摊薄成本假象校验 —— 「跌」与「亏」是两个问题**。① 先回数据核对**区间涨跌**（用户描述"一路跌"，实测近 1 年 **+5.35%**、近 6 月 **+12.27%**、位置分位 **66.4%**，−8% 属正常波动，年化波动 23.68%）；② 再做**成本口径校验**：累计净值 ≡ 单位净值（差恒 0.0000）⇒ 无分红 ⇒ 由 `市值 ÷ 净值` 反推份额 ⇒ `反推成本净值 = 单位净值 × 成本 ÷ 市值`；③ 若 **反推成本净值 > 成立以来最高净值**，则该收益率必含"已减仓部分的已实现亏损"，属**摊薄成本假象**，真实浮亏更小（实测 017436：反推 2.7586 > 历史最高 2.5259，−16.01% 实际最多 −8.28%）｜死路：拿"累计收益 ÷ 当前市值"直接推回本线（分母错 → 回本线被系统性高估）｜来源：017436 止损决策｜计数：1｜状态：resolved

- 2026-09-17｜[LRN-20260917-001]｜场景：macro（宏观传导）｜经验：**宏观先算暴露面，再谈影响**。① 收到"美联储加息对我持仓有什么影响"类问题，第一动作是**算敞口**而非判断宏观：本币利率资产 **95.58%** / 直接美元暴露 **2.418%** / 本币权益 2.00%；② 量级用 **年化波动 ÷ √252** 折单日 1σ 再乘金额（实测合计 **±¥243.63 = 总资产 0.0149%**）；③ **方向反转**——真正受损的是**不能投的那部分**（活期宝超配 ¥584,479 缺美元通道，机会成本 **¥29,801/年**），而非持有的美元仓位；④ **利差 ≠ 汇率方向**：10Y 美债−中债 **334bp** 本该压垮人民币，但中间价 **6.7743** 反而调升 44BP ⇒ 升值源自**美元信用折价**，不是利差 ⇒ **不构成加仓美元资产的理由**｜死路：从新闻直接推结论（不先量敞口）、用"最坏情景"替代 1σ 判量级｜来源：美欧加息对持仓的传导分析｜计数：1｜状态：resolved

- 2026-09-17｜[LRN-20260917-002]｜场景：rotate 衍生（外币货基，**场景缺口**）｜经验：**路由表无「外币货币基金」分支**，现有 rotate 只管人民币货基（费率/规模/快赎）。外币货基须补 **3 项判据**：① **是否占 QDII 额度**（决定能否突破 ¥10K/月，是本类任务的**全部价值来源**）② **汇兑损益 vs 利差对账**（名义利差须减去人民币年化升贬，出敏感性表）③ **可申购性 / 起购 / 当前限购**（买不进即出局）。另：**同桶 ≠ 同类**——QDII 桶内美股股票与美元债风险等级差 **7 倍**（波动 23.68% vs 3.35%），相关性仅 **+0.209**、同向 **51.0%**｜死路：沿用人民币货基判据直接选外币货基（漏掉额度与汇率两个决定性变量）｜来源：挑选美元货币基金（`HANDOFF-2026-09-17-usdmm.md`）｜计数：1｜状态：pending（场景未回流，待用户批准）

- 2026-09-17｜[LRN-20260917-003]｜场景：bond（事件冲击穿透）｜经验：**加息对固收持仓的影响，要按久期分层算、并穿透"名义类别"**。① 用户问"加息对我正在搬去的债基有什么影响"，不要笼统答"利空债基"——**先按久期四型分层**（中短债/短债 88.91%·滚动持有 2.07%·长期纯债 4.71%·二级债基 4.32%），久期短的那 88.91% **几乎不受影响**（AAA 1Y 票息 1.5203% 是合同收益）；② 受影响的是**资本利得**一层：长端原本可预期下行 20bp → 汇率约束后约 7.5bp，损失 **12.5bp × 久期敞口 ¥545,778 = ¥682/年**（占债基 0.140%、占年化目标收益 1.5%）；③ **真正受损的是含权益那只**——110027 标签「债券型」实为二级债基、股票 **19.72%**，重仓半导体/光模块（正是美债利率上行的承压名单）；④ **代价 vs 收益要对照量级**：加息代价 ¥682/年 vs 搬活期宝超配收益 ¥4,676/年 = **6.85 倍** ⇒ **加息不改变搬家方向，只微调终点排序**（ETF 尤其银行/红利 ↑ ＞ 债基 ↓ ＞ 美元资产 ✗）。**方向判据**：一句话错位会翻转结论——「利率**被压低**」→ 债基**受益**；「利率**跌不下去**」→ 债基**轻微受损**。用户原话"加息意味着人民币利率被锁在低位"属后者，须先纠正因果方向再答。"
｜死路：把"债券型"标签当纯债（漏掉 19.72% 权益）、只看总量不看久期分层（会把 ¥682 的代价说成重大利空）｜来源：加息对债基持仓的影响（`_rate_bond_0917.py`）｜计数：1｜状态：resolved

- 2026-09-17｜[LRN-20260917-004]｜场景：cross（**「同桶 ≠ 同类」跨任务复发，计数 1→2**）｜经验：**分类桶相同 ≠ 风险同类——下单前看实际资产占比，不看分类标签**。首次（LRN-20260917-002）：QDII 桶内美股股票 vs 美元债，波动 **23.68% vs 3.35%**（差 7 倍）、相关性仅 +0.209、同向 51.0%。复发（本次）：`bond` 桶内纯债 vs 二级债基，波动 **0.61% vs 9.16%**（差 **15 倍**）。**两次都因"只看桶名"而险些误判** ⇒ 已具备升格条件（跨 2 任务复发），**待提纯升格进 SKILL.md 正文**（第 2 步"穿透校验"即为本次落地形态）｜死路：用桶名代表风险等级｜来源：美元货基遴选 + 加息对债基影响｜计数：2｜状态：monitoring（待第三次复现或用户确认后升格正文）

