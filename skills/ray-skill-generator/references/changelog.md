# ray-skill-generator 维护记录

> 从 SKILL.md 卸载（2026-09-01 v1.10.0）——遵守本 skill 教别人的"维护记录不常驻正文"原则（自举）。按需加载。

| 日期 | 变更 |
|------|------|
| 2026-09-08 | v1.12.0：进化轮（对比官方同类 skill 创建器与外部制作器，克制吸收 4 项）。①**Phase 0 新增「问题契约 + 行为契约」**——澄清结论落纸（反复出现的问题/完成证据/不处理的近邻问题 + 必须做到/禁止出现/允许变化/关键失败），把"好用聪明"改写成可观察行为，"不适合沉淀"当场劝止；②**触发双向验证量化升级**——重要 skill 建 ~20 条静态评测集（should/near-miss 各半），60/40 训练/测试切分防过拟合；官方经验：模型有 undertrigger 倾向，description 适度 pushy（明写相关场景）比堆关键词有效；③**Phase 3 新增「分级验证等级报告」**——结构校验/行为冒烟/留出回归（盲评）/安装交付 4 级，按实际达到的最高等级汇报，留出样本扣下不给执行者看；④**重复造轮子信号→固化 scripts/**——多个测试各自写出相似辅助脚本 = 该逻辑应 bundle 进 scripts/。未吸收：eval-viewer/量化 benchmark 基建（pass_rate/tokens 统计，依赖子代理跑批基建，WorkBuddy 环境暂无，记为方向）、盲评 A/B 对比（同因）、更新已有 skill 须复制到可写位置（WorkBuddy 技能目录本就可写）。 |
| 2026-09-02 | v1.11.0：进化轮（对比 GitHub evidence-driven skill creator 外部 Top 候选，克制吸收 1 项）。**「增量价值验证」升级为「RED/GREEN 双门控」**——RED Gate：先无 skill 跑基线，记录基线失败，无失败则 skill 无存在必要；GREEN Gate：有 skill 后必须清除全部基线失败，全清才成立（把原"with vs without 对比建议"升级为强制行为差门控）。配套新增 **evals/ 随 skill 交付**——GREEN 通过的测试 prompt 存进 skill 的 evals/ 成为回归测试套件（下次优化后可重跑），与 ray-skill-evolve 冒烟互补（evals 测行为/冒烟测脚本）。反模式黑名单补 1 条（RED 基线无失败仍硬建 skill）。未吸收：lint validate_skill.py 自动校验（health_check 已覆盖）、ecosystem doctor（跨 skill 冲突扫描超出单 skill 职责）、cross-runtime compile（本地单 runtime）。 |
| 2026-09-01 | v1.10.0：进化轮（对比 skill 创建领域外部 Top 候选 10+ 与官方 spec，克制吸收 3 项）。①**Phase 3 新增「量化评分闭环」**——9 维评分表（触发词覆盖/流程清晰度/降级策略/描述质量/示例完整性/错误处理/文档结构/可测试性/复用性），<7 分当场补强，与健康分配合（总分 + 短板定位）；②**Phase 2 新增「环境变量占位符规范」**——真实值一律 `{{VAR_NAME}}` 占位，禁止硬编码（防泄露 + 可移植）；③**Phase 2 新增「禁止文件清单」**——skill 目录不建 README/CHANGELOG/QUICK_REFERENCE 等杂项文件（官方 spec 权威）；④反模式黑名单补 2 条；⑤description 补硬约束（不含 `<>`、≤1024 字符）；⑥实战经验区卸载至 references/experience-archive.md。未吸收：eval 自动化闭环（本地人工验证流程已覆盖）、自动技能提取脚本（属反思类 skill 领域）、记忆 namespace 隔离（属反思类）。 |
| 2026-08-29 | v1.9.0：经验回流修正（用户指出 v1.8.0 回流太窄——只停内容层）。**新增「工具准备与可持续进化」通用原则（Phase 1）**：创建任何 skill 前，先按**具体功能**想清楚"要准备哪些可执行工具"（scripts/），且工具必须设计成**可持续沉淀、优化、增强的活资产**——自带反馈回流口（任务中发现边界案例→修正脚本）、版本化（每次进化 bump）、可测试（跑一次验证输出）。判断口诀："这个 skill 的能力靠什么实现？"靠脚本→scripts/ 做主体；靠知识→references/ 做主体；靠模板→assets/ 做主体——别让 SKILL.md 同时承担知识和工具。检索类设计特例被重新定位为"工具层原则在内容层的应用"；实战经验追加 LRN-20260829-003。 |
| 2026-08-18 | v1.6.0：进化轮（吸收"弱模型适配"写作法，克制吸收 1 项合并包）。Phase 2 构建新增「弱模型适配写作三件套」——输出格式模板（带占位符，不写"写清楚点"）+ Few-shot 输入输出示例（1-2 个，弱模型靠模式匹配跑好）+ 结构化标记（XML/Markdown 标签做定位锚点）；反模式黑名单补 1 条（自由发挥指令）。未吸收：复杂步骤拆分（Workflow 类型已覆盖）、硬约束/自检（红线+DoD 已超额覆盖）。 |
| 2026-08-17 | v1.5.0：进化轮（对比 skill 制作领域外部 Top skill，克制吸收 1 项）。Phase 2 新增「Definition of Done 章节」（模板必带可测完成标准 + When Not to Use 边界章节）——让 skill 完成从"感觉"变"测量"，Phase 3 验证直接对照 DoD 逐条打勾；反模式黑名单同步补 1 条。未吸收：轨迹编译器/Skill Naming 细化/When-Not-To-Use 独立工具（与现有机制重复或重型）。 |
| 2026-08-17 | v1.4.0：尺寸阈值软上限化（配合 ray-skill-evolve v2.10.0）。①渐进式披露 Level 2 目标 <500 行→<600 行软上限 / <5000→<8000 token，补"信息密度优先，不为压行数砍内容"；②反模式"超 500 行不卸载"改为"超 600 行仍不处理"，并注明软上限排查顺序（冗余→结构→职责→token→行数） |
| 2026-08-16 | v1.3.0：验证闭环升级（对比外部 skill 制作领域 Top skill，克制吸收 2 项）。①Phase 3 新增「触发双向验证」——should-trigger 3 个 + near-miss 边界模糊反向用例 3 个，证明 description 写得准；②新增「增量价值验证」——with-skill vs without-skill 对比执行，证明 skill 有增量（不是模型本来就会）。未吸收：跨平台分发/eval harness/Evidence-based 哲学（重型或与现有机制重复）。 |
| 2026-08-15 | v1.2.0：脱敏左移（用户要求）。①Phase 2 构建新增「脱敏三规则」（不写来源署名/不写个人隐私/维护记录不列第三方名）——创建时就内建脱敏，不等发布前才查；②反模式黑名单补 3 条（来源署名/个人隐私/维护记录第三方名）；③init_skill.py 模板维护记录区加脱敏提示。与 ray-skill-evolve 的发布前脱敏形成双保险。 |
| 2026-08-15 | v1.1.0：进化轮（对比 skill 制作领域外部 skill）。①frontmatter 补可选字段 alwaysApply / disable-model-invocation + description 禁止写工作流总结；②新增「Skill 构建陷阱」5 条（解释X是什么/关键词堆砌/模板内联/模糊指令/未声明文件创建）。 |
| 2026-08-15 | v1.0.0：融合创建。整合六步创建流程 + 五阶段质量审计 + 四类型分类 + 安全审查。复用 ray-skill-evolve 的 health_check/sanitize 脚本做验证，不重复造轮子。 |
