# ray-translate 版本历史

| 日期 | 版本 | 变更 |
| 2026-09-23 | 4.1.0 | **方向三新增「语言侧五条硬约定」（PGS 3 紫皮书 237 页实战，self-neat 回流）**：版式保持型路线迁出几何方法论后（v4.0.0），本 skill 保留的「按契约填译文池」一侧**缺一份判据清单**——PGS 3 证明这五条属语言侧、写进任务书才生效：① **指数/上下标一律保持 ASCII 原样**（`10-8`/`m-3`/`s-1`/`y-1`），**严禁**改写为 Unicode 上标（源件用缩小 span 实现上下标、文本层本就压平；且华文中宋/微软雅黑/宋体/黑体**全缺** `⁻`(U+207B) 与 `⁰⁴⁵⁶⁷⁸⁹`，直写即黑块；仰角由几何侧 RF-19 复刻）② **目录行只译标题、点导引与页码逐字符保留**，必须**脚本正则切出并程序化复制**（禁手抄上百句点）；多行 toc 用 `\n` 保源行结构 ③ **`kind=cell` 只译有实义词**（站名/风向代码/专名/数值/符号原样；`Day`→昼间、`Night`→夜间、`Total`→合计）④ **`kind=reference` 保留原文著录**（学术惯例 + 与 PGS 2 交付件一致；⚠️ 提取器只认独立标题行，`5. REFERENCES` 带章号会漏判，任务书须显式交代）⑤ **窄单元格压缩授权**（`<>"geen"（不等于"无"）`→`<>"无"`；数字/单位/否定/物质名不可删；压缩归译者权限、几何侧不得代做）。同步修正维护记录尾行的版本指针（原写 v4.0.0，实际 frontmatter 已到 4.0.2——属"记忆速查区版本 vs 实际"失同步，本节顺手修） |
| 2026-09-21 | 4.0.2 | 术语库硬顶 1500 → **1800**（用户定档：建库阶段池子宜大，靠任务筛选沉淀高价值词，稳定后可再收缩）。同步更新正文两处上限描述。纯参数调整，无方法论改动。 |
|------|------|------|
| 2026-09-21 | v4.0.1 | **排版降级链（PATCH，用户裁决"ray-translate 保持独立性"）**：方向三导语新增降级链——版式保持路线优先交棒 ray-file-inplace；该 skill 未装载/缺席时**降级退守原输出**（spec→DOCX→Word COM 导出 PDF，同目录同类型 v3.3.2 规则不变），退守产物过基础验收（页数/U+FFFD/TOC 域刷新抽读）并标注"深度排版审计未跑"，ray-file-inplace 在场后可接管深度修复。互为独立性背书：两 skill 皆可单独作战，在场时走契约协同 |
| 2026-09-21 | v4.0.0 | **结构性拆分（MAJOR，用户裁决"翻译与排版分家"）**：版式保持型 PDF 原位回贴子流程（v3.6.0）+ 交付后精修与体积纪律（v3.7.0）**整体迁出至新 skill `ray-file-inplace`**（文件原位回贴与排版修复，格式无关核心层 + pdf 适配器 + 译文池契约），并并入 PGS 2 v9.2 实战教训（吞了没补 / 行内符号随行重译 / 不动点合并 / 译文池基线链 / 通道冲突消解与 superseded 实删）。本 skill 保留：方向三重排型五步流程、验收与复核纪律、法律文书纪律、插图回插子流程、CAT 三段式与术语库；在版式保持型路线上只按契约填译文池（`frag_jobs/dN.json` → `frag_out/dN.json`）。原两节位置各留指针。description/方向三导语/维护记录同步。**v3.6.3 占位条目仍未解，本条不代填** |
| 2026-09-20 | v3.7.0 | **方向三版式保持型：交付后精修与体积纪律 + 碎片段归因与定组铁律（PGS 2 黄皮书 870 页布局改造实战，self-neat 回流）**：①**字体资源名必须隔离**——常规/粗体各用独立 `fontname`，只换 `fontfile` 会让**同页首个注册的字体通吃整页**（指纹＝**整页单一字体、0 页混合**；实测 55 页正文整页变粗，**旧交付件同样存在**；反向亦成立：本该粗体的标题丢粗体）；目录类后置脚本须用第三个名字 ②**交付前必跑 `subset_fonts()`**——`insert_text(fontfile=…)` 是**逐页**嵌入子集，粗体从 55 页扩散到 410 页后文件涨 8 MB；`subset_fonts()`+`save(garbage=4)` 实测 **33,650,778→7,071,445 B（−79%）**，页数/`get_drawings`/`get_images`/抽样文本量逐项不变 ③**行距不能沿用源行网格**（拉丁正文 10pt/12pt=1.20 vs CJK 字面框≈1.16×字号 ⇒ 行行相接，实测"行距不足"9,092 处）→ 改**按可用高度重分布**（下限 1.15×、上限 1.45×字号；可用高度受「下一单元上沿」与「原块自身范围」**双约束**，后者防把行撑到插在中间的图/表上）④**碎片段归因四类 + 定组铁律**：四类＝首行缩进断裂 / 同视觉行拆段 / 标签+符号+描述三段拆行 / 载体误判（整页判 `reference`）；修法＝**并成"整段补丁"重译且不动提取器**（id 不变 ⇒ 其余译文零返工）。铁律：**同栏＝两行 x 范围相交**（禁"x0 相等"漏首行缩进整类；禁"起栏带"——会把左右双栏串成一段致**真实叠印**）；**组须几何干净**（无组内非成员行/无组间相交/单元不重复归属，"吸收冲突"须配并查集合并）；**相邻判据用行基线 `oy`**（修订说明页 y 盒互相重叠，用 y 盒全页判不出相邻）；**标题护栏不能只看字号**（行首"–"把正文字号抬到 14pt ⇒ 误杀整页）；**`kind=equation` 永不参与行内合并**；🚨**先体检再翻译**（重复归属 0 且组间相交 0 才投译文）。⑤**v3.6.3 占位条目仍未解，本条不代填** |
| 2026-09-15 | v3.6.3 | ⚠️ **版本号已 bump，变更未记（失同步，2026-09-16 跨会话核验发现）**——依据：`SKILL.md` 末次修改 **2026-09-15 22:17:43**（晚于本 changelog 末次写入 21:56:26），而 frontmatter `version: "3.6.3"` 与本表、SKILL.md 维护记录指针（均停 v3.6.2）不符；核验命令 `python ~/.agents/skills/ray-neat-freak/scripts/snapshot_check.py`。**本条为占位：内容待原会话补记，不得据 "3.6.3" 推断变更内容。** |
| 2026-09-15 | v3.6.2 | **方向二新增「版面装配」方法 C（用 PowerPoint 自身排版引擎定字号）+ 段落内软换行丢内容陷阱（90 页 / 2.2 万字生产制度培训课件英文化实战，self-neat 回流）**：①**判据必须用目标软件自己的排版度量**——自写 em 估算（CJK=1.0em / ASCII=0.52em / 行距 1.2）实测偏差约 **1.5 倍**（模型报「需缩放 0.51」而渲染显示中文仅占版面 76%）→ 改 COM `TextFrame2.TextRange.BoundHeight` + `PageSetup` + `Slide.Export` 渲染抽查；②**可用空间 ≠ 文本框高度**（本类 PPT 文本框普遍小于文字实际高度，中文本身就「越框」渲染）→ 边界 = min(外层卡片底边, 下方最近文字块顶边, 页面底边)；装饰图形与 **≤2 字符编号徽标不得当边界**（否则算出 avail = 6~19pt 假预算，实测连踩 6 页）；③**生长方向**：spAutoFit + anchor=ctr 时英文变长会把框**向上长**压住标题（第 25 页实测标题正文重叠）→ 文本高于框时改 anchor=top；判据用几何，**不要只测 `autosize==1`**（出事的框 COM 报 -2 mixed，恰好漏掉）；④**压缩与字号分工**：压缩目标长度由实测算出（`avail/(floor×boundH)`；数字/单位/时限/标准号/否定/责任人/安全动作一律不可删——85 条压缩后对源逐项核对三者零丢失），按页统一 `S=clamp(min(s_need),0.78,1)`、单容器下限 0.62；**写回字号用 COM**（python-pptx 的 `font.size=None` 是继承值，按其缩放基准会错）；⑤**新陷阱：段落内软换行**——源 `<a:t>` 内嵌 `&#10;`/`&#13;` 表示同段多个可视行，按「一段落=一条译文」写回会**只写首行、其余静默消失**（第 9 页丢 7 条约 1100 字符），而中文残留 0 / 数字对账 / 行数守恒 / replaced units 全绿 → 译文按各段行数 `L_i` 切分回填并断言 `len(译文行) ≤ sum(L_i)`（少了可 pad、多了必中止）；段内多行用与源同编码，**不要 `<a:br/>`**（just 对齐下会拉伸出巨大字间距）；⑥COM 另存后**媒体部件数下降属假报警**（89→56，PowerPoint 归一化重编码）→ 判据改用 `<p:pic>` 形状数（159=159）与 rels 可解析性 |
| 2026-09-14 | v3.6.1 | **译前术语提取程序化（"一次做对"的关键杠杆）+ 四库扩容**：①新增 `scripts/extract_terms.py`——源文档 → 句内 n-gram 提取（页覆盖样板词过滤/程序词单位过滤/碎片防护/单复数归一/倒排索引，1.5 M 字源 3.5 s）→ 自动对四库 → 输出 `*_locked.md`（命中，直接作为 spec 术语节，**禁手写**）+ `*_gaps.md`（缺口核对清单，**不按排名截断**，否则低频领域词被挤掉）②缺口**三分类处置**（领域专有词必须权威源核验 / 通用词可直接采用 / 噪声剔除）③CAT 三段式 ① 改写：明确「**库只在译前提取步生效**，入库≠自动生效」+ **禁人工挑词**（实证：PGS 2 手工挑 112 条漏 39 条；且挑入的 `congestion`/`impoundment` 源文出现 **0 次**——源文实为 `obstructed region` 203 次 / `bund` 40 次，属"领域知识带入"而非源文用词）④四库硬顶 1200 → **1500**（PGS 2 入库 74 条后余量仅 10%）⑤`--selftest` 自证三件套（非空断言 + 正向控制 + SABOTAGE 负控）⑥**新增 `references/term-verification.md`**（补上 ① 中已写的引用目标，消除死引用）——固化权威源清单（AQ/T 3046 / SH/T 3226 / GB/T 37243 / GB/T 23694 / DB32/T 4745 / T/COSHA 71 及本地盘+IMA 双路径）、核验方法优先级（文本层检索 > 渲染读图 > 交叉章节检索）、三条死路（术语在线 API 需令牌 / CNKI SPA 空壳 / tesseract 是 js 垫片）、备查池错域误用表、跨标准分歧处置（个体风险 vs 个人风险、液滴降落 vs 液滴沉降、BLEVE 两式并收、containment 双义语境限定）、回写 fail-closed 四步与**守恒式恒等式 `Δ = 新增 − 替换`**。验收：40 条人工基线覆盖 37 条，未覆盖 3 条经查证系源文缺失/输出截断，非工具之误 |
| 2026-09-14 | v3.6.0 | **方向三新增版式保持型 PDF 原位回贴子流程（PGS 2 黄皮书 870 页/14,392 单元全量实战，self-neat 回流）**：①适用判据——公式/图/表格/页码密集的版式敏感手册走「redaction 挖除+原基线回贴」而非 spec 重排（重排型公式必走样）②提取三判据——两端对齐 run 合并（同 y 间隙<12pt）/段落合并按字体族（比例可并、Courier 等宽永不并；左对齐「顶右边界」判据失效）/行字号取字符加权众数（项目符号抬高 max 致叠字）③冻结纪律——翻译开工前冻结提取器（中途改判据=id 全变=已译作废）④回贴——自实现逐字折行；中文字体句点≈Plantin 2 倍→目录点导引必须吃点收一行；head_only 只挖标签保对齐列；译文==原文整行不动；绝不截断⑤字体——正文华文中宋+标题微软雅黑 Bold（fsType=0x0008 允内嵌），符号等价替换按所选字体字形覆盖动态决定（华文中宋原生 ∆˚ς−）⑥QA——页数/drawings+images 计数/成品重提取分类非参考文献区残留==0/行级守恒/缺字形按实际嵌入字体查。QA 实测：残留英文 0 行、溢出 0.16%、覆盖 808 页 36.2 万字 |
| 2026-09-13 | v3.5.0 | **方向三并行分片完整性对账 + 术语库检索判据修正（self-neat 回流，EEMUA 191 第 3 版 274 页 / 216 页交付实战）**：①**验收与复核纪律 +4 行** —— **分片围栏完整性（J8）**：畸形代码围栏（指纹=围栏标记与 `json` 之间被插入换行）计数必须 == 0，且 `{{TABLE n}}` 占位数 == json 围栏数（实测 3 文件中招，而合并对账/体积/行数/标题清单**全都正常**，常规判据抓不到）；**单元内容逐行守恒（J9）**：预处理全文 vs 该单元全部分片拼接，去围栏/JOB 头/占位行后**正文行逐行相等**（30/30 逐字过，比「块数相等」强）；**表格行数判据口径**：须写 `(1 if headers else 0) + len(rows) == len(输入 rows)` —— 输入 JSON **无 headers 字段、第 0 行即表头行**，漏算即整批假 FAIL（首跑假 FAIL 33 项全源于此）；**表格豁免须留痕**（具名字典 表号→理由+类别，禁整体放宽）。②**术语库纪律 +4 条** —— **EN 匹配改「词边界子串」禁「EN 单元格全等」**（旧判据 `k in en_set` 把 6 个复合词条下的词误判「缺」，**报缺 45 项 vs 真缺 36 项，高估 20%**）；**英式/美式拼写并收**（EEMUA 用 `rationalisation`/`prioritisation`、ISA-18.2 用 `rationalization`/`prioritization`，两种拼法都要能命中，否则英式源整片漏检）；审计输出**必带正控+负控**；入库判据用**守恒式**（旧行逐行仍在 + 新行数==追加条数 + 节数+1）+ SABOTAGE 删行负控。③**故障表 #10 补全命令序列**：导出前依次 `Fields.Update()` → `TablesOfContents(i).Update()` → `Repaginate()` 再 `ExportAsFixedFormat(out, 17, CreateBookmarks=1)`，**必须「先更新域后重排」**（顺序反了页码仍是旧的）。④**插图回插补图题口径**：图题译中文、**图内标注文字保留英文**。⑤**条数声明同步**：safety ~375→411、库合计 ~967→1003（EEMUA 191 新增 36 条入 safety 十三节） |
| 2026-09-12 | v3.4.0 | **方向三六项回流（self-neat 回流审计 + AAR，报警管理 ISA-18.2 74 页实战）**：①**OCR 无文字层分支**——提取后先验字符数（≈0 字符/页即判无文字层），`dtool ocr`/`tesseract` **分页批处理**（每批 10-20 页；整本一次跑必超时且失败无提示）+ 超时降级三招（450 dpi 重渲染 / `psm 4` 与 `psm 6` 双跑取优 / 逐页分批落盘累加可续）+ 英文源 `-l eng` + OCR 产物结构校验（页码连续/条号 1→N，OCR **静默吞行**是常态）②**新增「验收与复核纪律（方向三特有）」小节**——**源↔成品结构化对账**（成品内部「条号 1→N 连续」在**整体缺一章**时照样成立 → 必从源与成品各抽结构清单逐项比对并归因）＋**判据语义限定**（全文 `count()` 会把正文里作为反例**引述**的旧译法算成命中 → 行首锚定/排除引述段，判定前先 `repr()` 复核）＋**交付件级回查**（判据落最终 PDF/DOCX 而非中间 spec）＋**独立实现复核**（关键判据用第二实现复算）③**故障表 +2**：#9 中文路径下脚本静默崩（exit 0/1、**零输出无 traceback** → 先 cp 到 ASCII 临时目录再跑、结果写 JSON 落盘不靠 stdout、Python 内路径用 raw 字符串）、#10 Word COM 导出**不刷新 TOC 域**致中文 PDF 目录页码不符/整块空白（导出前 `doc.TablesOfContents(i).Update()` + `Repaginate()`，导出后抽读目录页验收）④**术语库纪律 +2**：回写一律走 **fail-closed 脚本**（每处 `count(old)==1` 断言，任一不符整批不写）＋ SABOTAGE 负向自测；**拒用❌列禁止自造**（须权威源或实战实证依据，无依据**留空**）⑤触发条件 +1（扫描件/无文字层 PDF → 中文）⑥**条数声明更正**：safety ~293→375、chem-eng ~228→238、chem ~184→187、四库合计 ~800→967（声明自 v3.1.0 起未随入库同步，属漂移） |
| 2026-09-09 | v3.3.3 | **方向三新增插图回插子流程（CHEF Manual 189 页/71 图全量实战）**：①探针定界——fitz 真题注块检测（短块 ^Figure X.Y，防正文交叉引用误判页码），区域=[max(上一题注底,上一正文块底)+4, 题注上沿-1]，题注在页顶→图在上一页底 ②提取——内容框=栅格+矢量+短文本块并集，单栅格≥70% 用 extract_image 原生字节否则 220dpi 渲染 ③python-docx 插入——**width+height 必须同给**（fitz PNG pHYs=0，只给 width 读 DPI 除零），题注去「原图略」保首 run 格式 ④QA——插图计数/U+FFFD=0/5 张多模态对位/译者说明同步改写。特殊裁决：原书重号图各自插图+译者注、同图双插、无编号图同源去重「参见图 X.Y」、源书无图保留原状、表格型图用户定。两坑：题注与轴标签合并文本块（下沿=区域下沿）、题注块压栅格 bbox（下沿外扩栅格底+2） |
| 2026-09-08 | v3.3.2 | **交付目录默认规则（用户明确，CHEF Manual 189 页翻译任务提出）**：翻译生成的文件**默认放在源文件同一文件夹下**，除非用户另有明确指令；中间产物仍留临时目录（v3.2.2 纪律照旧）。SKILL.md 开工简报规则段新增一条 + 方向三第 5 步同步标注"交付目录默认 = 源文件同一文件夹" + description 同步 |
| 2026-09-08 | v3.3.1 | **方法 B/B+ 两处回流（self-neat，148 页设备课件二次全量实战）**：①方法 B 格式保留加**段落级写回铁律**——整框替换禁止"整框文本压进首 run"（多段/编号列表框会被压成单段+字面换行丢结构；en2en 返工事故实证，靠从源文件全量重建修复；正确做法=逐 paragraph 替换）②方法 B+ 子代理 prompt 补**长度预期内嵌**（EN/ZH 字符比 2.5~3.5x 正常、视觉 1.3~1.8x，勿压长度删技术内容，溢出 fix_overflow 兜底）——11 代理并行时 4 个代理在报告里反复解释"3x 超长"，属 prompt 预期缺失症状 |
| 2026-09-08 | v3.3.0 | **方法 B+：超大 PPT 并行子代理翻译（self-neat 回流审计，148 页设备课件实战）**——方法 B 四步法只覆盖单进程顺序路径，>100 页/>5 万字的 PPT 会超长。新增方法 B+ 八步流程：全量导出→章节切分→主进程建翻译规范（栏目/术语统一保障）→并行派子代理（每章 1 代理产 chXX_dict.json，不碰 PPTX 防并发写）→主进程合并→apply_dict 就地替换（可重复跑安全）→fix_overflow 溢出缓解→每批 QA。实测：148 页 10.7 万字 2 子代理并行完成第 1 章，用户验收 OK。要点固化：子代理 prompt 自包含/重复文本自动合并键/先做样板再批量 |
| 2026-09-06 | v3.2.3 | **spec 程序化构建铁律（self-neat 回流审计，AB Specialty 测试教训）**：方向三第 2 步加铁律——spec 必须 Python dict + json.dump 程序化构建，禁止手写 JSON（手写必踩内容 ASCII 引号截断坑，报 `Expecting ',' delimiter`；状态机自动修复会搞坏文件，曾 3 次失败）；中文内容引号用「」；构建脚本分批 append 落盘。故障表补 #8 |
| 2026-09-06 | v3.2.2 | **交付目录纪律（用户实战澄清）**：交付目录只放与原格式相同的文件——源 PDF 只交付中文 PDF，中间 DOCX/spec/提取文本留临时目录不进交付目录（AB Specialty 测试中误把 DOCX 副产物放交付目录，用户指出"原格式不是只有 PDF 吗"后修正）。规则段新增交付目录纪律；方向三第 5 步与交付物清单同步（DOCX 仅 QA 与导出桥接，用户明确要才给） |
| 2026-09-06 | v3.2.1 | **输出格式默认规则（用户明确）**：译文输出格式默认与输入原格式相同（PDF→中文 PDF、DOCX→DOCX、xlsm→xlsm），除非用户明确指定新格式。开工简报下新增规则段；方向三第 3/5 步改为"DOCX 为中间载体 → 最终按源格式还原交付"；标题/导语/分工/触发词/description 同步去"Word"锁定表述（方向三典型源为 PDF，默认出中文 PDF，DOCX 保留为可编辑副产物） |
| 2026-09-06 | v3.2.0 | **新增方向三：英→中 PDF/法规/长文 → 中文 Word**（ray-skill-evolve 优化轮，09-04 OSHA Act 514 + HIRARC 实战经验回流——SKILL.md 此前只覆盖 Excel 英译中 + PPT 中译英，PDF/法规英译中靠临时拼凑流程）。①新增完整工作流章节：五步（ray-doc-toolbox 提取 → CAT 术语锁定 → 译成 `*_zh_spec.json` → create-docx 生成 → QA 抽读验证）；②法律文书翻译纪律表（shall→应/须、so far as is practicable→在合理可行范围内、罚则金额/日期/机构缩写原样保留、修订脚注保留、已删除条文照译标注、文首版本说明段）；③触发条件 + description 补方向三触发词；④故障表补 #7 生成类命令静默失败（QA 抽读与生成成对，不能只看 ok:true——09-04 实测 spec 字段名错/草稿残留致内容残缺零报错）；⑤交付物清单补方向三产物。displayName 不变（仍中英双向含过程安全） |
| 2026-09-06 | v3.1.0 | **动态库改造四件套（用户批准）**：①规则派生型补库 277 条（chem 酸碱盐/同系物/官能团/聚合物 +102，chem-eng 泵阀/换热内件/容器附件/仪表 +99，safety PSM 要素/危害分析方法/毒理/应急 +76），四库达 797 条，硬顶 1200 ②动态库机制：入学=任务审计入库 / 升级=热词表 `_hot.md`（译前默认注入，只收无歧义词，上限 80）/ 毕业=`_graduated.md`（仍可 grep，永不删除）；频次信号=任务命中台账 `_hits.md`；攒 10 任务或月度调整 ③裁决优先级插入备查池数据集一级（CNKI 后、书库前）：候选池不裁决，收录旧译名（矽/比重类）须回 1-3 级验证；新增 `scripts/query_fallback.py`（EN↔ZH 精确/前缀/子串查询，支持 -d 域限定）④格式不变：六列 schema 单行一条 |
| 2026-09-06 | v3.0.0 | **领域扩展化学化工 + 常驻术语库**。外部对比（andrewyng/translation-agent 两遍式、Trados/memoQ termbase 三段式、Lokalise/Smartling 只注入命中词条、baoyu-translate 译前提取差距）→ ①displayName 化学化工翻译（中英双向，含过程安全），领域三层=化学基础→化学工程→过程安全（既有资产全保留为第三层）②新建 `data/termbase/` 四分域（core/chem/chem-eng/safety），六列 schema（EN/推荐ZH/拒用❌/语境辨析/来源/日期），EN-GLOSSARY 129 条全量迁移（原文件归档 references/archive/）+ CSB AB Specialty 报告词频校准新增（reactor❌反应堆/tank❌坦克/check valve❌检查阀/tray❌塔板/steam 蒸汽 vs vapor 蒸气/酯≠脂/胺≠氨/黏度用黏），grep-only 永不整读 ③CAT 三段式工作流（译前 grep 提取命中锁定→译中只注入命中→译后拒用零命中+收尾术语审计入库）④术语裁决优先级（术语在线《化学名词》2016 二版/《化工名词（四）》2020 > 国标 openstd > CNKI > 本地书库 > 通用词典）⑤新增 references/chem-conventions.md（物质命名/单位数字 GB 3100/3102/缩写规则/Don't-translate 化学专项扩充）⑥新增 references/book-shelf.md（5 本书目录卡，裁决层定位，扫描版按需 OCR 查页）⑦备查池 github.com/1999XIAOZHANG/chemical-glossary（15.1 万条）下载至 `~/.workbuddy/local-refs/`（技能外路径，仅候选永不直接采用）。注：《安全科学技术名词》独立卷查无实据不引用；IUPAC Gold Book 仅英文定义不作中文依据 |
| 2026-09-02 | v2.5.0 | 外部对比（多格式文档翻译候选）补 **sdt 元素处理**——Don't-translate 保留清单新增 sdt 元素（Word 结构化文档标签：封面标题/规范性引用/交叉引用）说明：内容译、结构标签保留；工具自动跳过 sdt 内容时需手动补译封面/引用区（外部实测 sdt 处理不当整段丢失）。 |
| 2026-08-29 | v2.4.0 | 外部对比（translation 96.3/localization 92.2/translate 同源×3）：新增开工简报 5 要素（用途/受众/语气/术语库/约束）；错译黑名单瘦身为最高危 Top5（完整 11 条回 EN-GLOSSARY）；提速策略 27→12 行压缩表；遗留注意事项去项目特定数字；术语评审闭环/核心函数两超长段收紧；维护记录卸载本文件 |
| 2026-08-12 | v2.1.0 | 外部对比同类学术翻译 skill 补①术语评审闭环（导出评审表给用户确认，修订版绑定）②第五阶段一致性检查（遗漏/误译/结构对齐/化学名）③编号澄清提问（不确定项编号问用户，不猜测定稿） |
| 2026-08-19 | v2.2.0 | **新增中→英方向（化工安全专业英文化）**——95 页化工厂入厂安全培训 PPT 英文化经验回流：①三层次原则（字面准确/术语精确/行业地道）②高频错译黑名单表 ③警示语 shall/should 语气规范 ④去 Chinglish 句式检查清单 ⑤英文长 40% 字号适配 ⑥`## Page N` 讲稿格式坑 ⑦机械 QA + 术语 QA 双保险；新增资产 `references/EN-GLOSSARY.md` |
| 2026-08-20 | v2.3.0 | 外部对比官网通用翻译 skill → 新增双向常驻「通用翻译铁律」（Don't-translate 保留清单 + 语义 QC）；结构瘦身：卸载 2 个大代码块到 `references/code-patterns.md`；新增「内容内嵌 PPT 就地翻译法（python-pptx）」；补故障 6（输出文件被 Office 锁定）；脱敏移除来源署名 |

## description 历史原文（2026-09-14 收敛前存档）

> 原 description 超 Anthropic 规范上限 1024 字符。为免信息丢失，**原文完整存档于此**；
> 新 description 只保留定位 + 触发词 + 路由映射，被砍的边界说明已外迁至 SKILL.md 的
> 「与其他 skill 的边界」节。

```text
翻译化学/化工/过程安全文档（Excel 工作簿 .xlsm/.xlsx、PPT 讲义/讲稿、英文 PDF/法规/技术文本/调查报告），三大方向：英→中 Excel 批量（正则整词匹配、VBA 宏保护、残留诊断）、中→英 PPT 英文化（术语精确+行业地道+安全语义无歧义、警示语 shall/should 语气规范、超大 PPT 并行子代理方法 B+）、英→中 PDF/法规/长文→中文（v3.2.0，spec 驱动 + QA 抽读 + 法律语气纪律；v3.2.1 输出默认与原格式相同，除非明确指定新格式；v3.3.2 交付目录默认为源文件同一文件夹，除非明确指定；v3.3.3 方向三新增插图回插子流程——占位译文把原版插图探查/提取/插回交付 PDF；v3.4.0 方向三补 OCR 无文字层分支与超时降级 + 源↔成品结构化对账 + 判据语义限定与交付件级/独立实现复核 + Word COM 目录域更新与中文路径 ASCII 中转 + 术语库回写 fail-closed；v3.5.0 方向三补并行分片完整性对账（围栏畸形==0 / 单元内容逐行守恒 / 表头行数口径 / 表格豁免留痕）+ 术语库 EN 匹配改词边界子串 + 英式美式拼写并收；v3.6.0 新增方向三**版式保持型 PDF 原位回贴**子流程（两端对齐 run 合并/字体族段落判据/字符加权字号/冻结纪律/五类专项补丁/字体嵌入授权与动态符号替换；v3.6.1 译前术语提取程序化——scripts/extract_terms.py 取代人工挑词（锁定表 + 缺口清单，缺口三分类处置；库只在译前提取步生效），四库硬顶 1200→1500；v3.6.2 新增方向二**版面装配**方法 C（PowerPoint COM 实测 `BoundHeight` 定字号/卡片底边=真实可用空间/锚点生长方向修正/段落内软换行对账/COM 另存媒体假报警））。v3.0.0 常驻术语库 data/termbase/ 四分域（core/chem/chem-eng/safety，六列 schema 含拒用❌标注，grep-only 永不整读）+ CAT 三段式工作流（译前 grep 提取命中锁定→译中只注入命中词条→译后拒用零命中+收尾术语审计入库）+ 术语裁决优先级（术语在线/国标 > CNKI > 备查池候选 > 本地书库 > 通用词典）。v3.1.0 动态库机制：热词表默认注入/低频毕业不删/任务命中台账；备查池 15.1 万条兜底查询 query_fallback.py（候选不裁决）。含双向通用铁律（Don't-translate 保留清单 + 语义 QC）与开工简报 5 要素（用途/受众/语气/术语库/约束）。适用于化学化工安全计算表/手册/培训材料/法规标准/事故报告的中英双向翻译。Use when: 翻译化工文档、xlsm 翻译、PPT 英文化、英文法规 PDF 转中文版。
```
